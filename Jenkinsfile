pipeline {
  agent any
  stages {
    stage('Build frontend') {
      steps {
        sh 'cd frontend'
        sh 'npm install'
      }
    }
    stage('Build frontend') {
      steps {
        sh 'cd frontend'
        sh 'npm install'
      }
    }
  }
}
